/*
	write a c program to print count of prime numbers between the given range.
*/
#include<stdio.h>
main()
{
	int i,n,count,sum=0,s,e;
	scanf("%d %d",&s,&e);
	for(n=s;n<=e;n++)
	{
		count=0;
		for(i=2;i<=n/2;i++)
		{
			if(n%i==0)
			{
				count++;
				break;
			}
		}
		if(count==0&&n!=1)
		{
			sum++;
			printf("%d ",n);
		}
	}
	printf("\n%d",sum);
}
